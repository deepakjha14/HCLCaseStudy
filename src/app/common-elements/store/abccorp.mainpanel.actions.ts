import { createAction } from '@ngrx/store';

export const loadPrice = createAction('[Counter Component] Increment');
export const loadGraph = createAction('[Counter Component] Decrement');
export const loadStocks = createAction('[Counter Component] Reset');